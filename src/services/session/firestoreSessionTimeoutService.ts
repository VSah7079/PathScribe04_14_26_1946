// Stub only — real implementation pending backend cutover.
// mockSessionTimeoutService.ts is the active implementation; this
// satisfies the service interface's contract so the swap to a real
// backend is a one-line change in services/index.ts when that backend
// exists.

// src/services/session/firestoreSessionTimeoutService.ts
